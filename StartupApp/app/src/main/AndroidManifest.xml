<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.genesisteam.apps.startupapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_green"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ChecklistActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BuisnessProfileActivity"
            android:label="BusinessActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".LessonActivity"
            android:label="@string/title_activity_lesson"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ProfileActivity"
            android:label="@string/title_activity_profile"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".InputActivity"
            android:label="@string/title_activity_input"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".CompletedActivity"
            android:label="@string/title_activity_completed"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MessageBoardActivity"
            android:label="@string/title_activity_message_board"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".BoardPostsActivity"
            android:label="@string/title_activity_board_posts"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>
